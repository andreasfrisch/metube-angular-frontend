'use strict';

describe('BlogArchive', function () {
	describe('Controller', function () {
		beforeEach(module('blogArchive'));

		// Initialize the controller and a mock scope
		beforeEach(inject(function () {

		}));
		
		afterEach(function() {
		
		});
		
		//it ('dummy test', function() {/*...*/});
		
	});
});